[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --strict-config
    -m "not integration"
markers =
    integration: hits a real CeForms backend; deselected by default (see tests/README.md)
